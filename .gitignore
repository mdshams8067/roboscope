# Environment — never commit real keys
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# Database — committed to repo so deduplication persists across GitHub Actions runs
roboscope.db-shm
roboscope.db-wal

# Run logs — generated output, not source code
runs/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# OS
.DS_Store
Thumbs.db

# Claude Code project instructions — local dev tool config, not for the repo
CLAUDE.md
